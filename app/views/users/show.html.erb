<div>
  <%= cl_image_tag @user.photo.key, :style => "width: 100px; height: 100px; border-radius: 50%;" %>
  <h2><%= @user.name%></h2>
  <p><%= @user.description %></p>
  <%= link_to 'Confirm Buddy', dashboard_path %>
  <%= link_to 'Back to results', destinations_path %>
</div>
