var walkId = '<%= @walk.id %>'

function toggleLikeButton(walkId, innerHTML) {
  let likeButton = document.querySelector(`.like-icon[data-id='${walkId}']`)
  likeButton.classList.toggle('#FD5C65');
}
toggleLikeButton(walkId);
